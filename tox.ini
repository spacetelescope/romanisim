[tox]
envlist= py38,style,bandit

[testenv]
usedevelop= true
extras=
    test

passenv =
    TOXENV
    CI
    CODECOV_*
    HOME
    CRDS_*

commands=
    !cov: pytest {posargs}
    cov: pytest --cov-report=xml --cov=src/romanisim --cov-config=setup.cfg {posargs}

[testenv:style]
deps=
    flake8
commands=
    flake8 --count

[testenv:bandit]
deps=
    bandit
commands=
    bandit -r -ll src

[testenv:build-docs]
extras= docs
commands=
    sphinx-build -W docs/source build/docs
